#Reacp


what is an exception ?
finally ?


5 keywords to handle exceptions -> try catch finally throw and throws

throw -> when you want to throw an exception explicitly .
throws-> when you want to delegate the responsibility of exception handling to someone else.


Custom exception must be an instance of throwable
after throw, code stops


WAP - > check the age if it is less than 16 then throw a custom exception (Invalid age exception) 
otherwise user can go for driving test. use scanner


# try with resource


20 min 

throw vs throws ? 




Collections -> framework to store and manipulate group of objects.

List                Set                   Queue                       Map
ArrayList
LinkedList 

It provides various methods to work on.
Collections works only with objects. no primitive data type can be used
Wrapper classes
int  ------------> Integer
double -----------> Double
float  -----------> Float

Generics 


Boxing -> primitive to wrapper 
unboxing -> wrapper to primitive


int x = 10 
float y = x


java is type-safe language.
String a = 1234;
JS,Python




Lombok -> reduce the boilerplate code


- WAP to create a container class which will have student(name,rollNo) and course(courseName) as a data member.
create container objects and print it.

- WAP to create a record class which will have 1 data member. and a getter setter method. in main create 3 records 
- one with student name, another with roll num and last with student percentage and in the end print all values.

