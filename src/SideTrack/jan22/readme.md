# .gitignore
- Tells which files to ignore (not to track)
- logs
- -build files
- compiled code

pr ? 
 add
commit 
push remote repo
PR - on github remote


clone 
pull vs fetch 
fetch -> download the changes only
pull -> fetch + merge  (merge conflict)



what happens after the code is pushed to github ? Deployment 


github --------------------- server -------------------- www.abc.com


mac dmg and windows .exe

jvm -> 

javac -> compile the code


platform independent ? for windows we need .exe and for mac .dmg

Student.java -------- java compiler (javac) ----------> student.class (byte code) -------> JVM
java is compiled or interpreted ?

JDk -> java dev kit (JRE + JVM)


.jar ->  java archive 
Maven -> automation build tool

deploy on any server but that server java on it.


CI/CD -> Continuous integration /Continuous delivery/deployment

Jenkins -> It is an open source automation server used to implement CI/CD pipelines.

CI -> Developers frequently integrate code in to a share repo.
- code is pushed multiple times a day
- Automatic build triggered
- automatic tests run


CD -> code is always in deployable state
Build + test + package
manual approval for production

jenkin file 

.jar
.war
.ear
